def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = [] 
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOrignalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0: 
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] 6 pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr

def getOriginalArray(pref):
	arr = []
	for i in range(len(pref)):
		if i == 0:
			arr.append(pref[i])
		else:
			arr.append(pref[i] ^ pref[i-1])
	return arr